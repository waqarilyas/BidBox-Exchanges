name: Deploy to Staging Server
on:
  push:
    branches: [staging]

jobs:
  deploy:
    name: Deploying to Staging Server
    runs-on: [ self-hosted, bidbox-vm ]
    environment: staging

    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          clean: False
      - name: Make environment files
        run: |
          cat << EOF > .env
            API_SECRET=${{ secrets.API_SECRET }}
            DB_HOST=${{ secrets.DB_HOST }}
            DB_DRIVER=${{ secrets.DB_DRIVER }}
            DB_USER=${{ secrets.DB_USER }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            DB_NAME=${{ secrets.DB_NAME }}
            DB_PORT=${{ secrets.DB_PORT }}
            ENCRYPTION_PASS=${{ secrets.ENCRYPTION_PASS }}
            
      - name: Building and Assigning Tag to image
        run: |
          docker build -t bidbox-exchanges:latest .
      
      - name: Removing old Container
        run: |
          docker stop bidbox-exchanges-service || true && docker rm bidbox-exchanges-service || true
      
      - name: Starting Docker Container
        run: |
          docker run -d -p 8001:8080 --restart unless-stopped --name bidbox-exchanges-service bidbox-exchanges:latest 
      
      - name: View Docker status
        run: |
          docker ps
      
      - name: Cleaning Server
        run: |
          docker rmi $(docker images --filter "dangling=true" -q --no-trunc)
